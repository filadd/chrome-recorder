# Dev build config — loaded by `npm run dev` and `npm run build:dev`.
# Points at the local Filadd gateway and reads the JWT from the local frontend.

VITE_API_BASE_URL=http://localhost:8000
VITE_AUTH_COOKIE_URL=http://localhost:3000
